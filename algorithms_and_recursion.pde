IntList pillars;
int index = 1;

void setup(){
  size(500,500);
  pillars = new IntList(10);
  totalPillars(pillars);
}

int totalPillars(IntList p){
  int pIndex = 0;
  if(p.size()<10){
    p.set(pIndex, );
  }
}
